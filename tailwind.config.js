/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class', // ← Add this (or 'media' for system preference)

    content: [
        "./src/**/*.{html,ts}"
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}


